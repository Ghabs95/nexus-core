{
  "name": "@nexus-arc/openclaw-nexus-command-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw plugin that forwards /nexus commands to the Nexus ARC command bridge.",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
